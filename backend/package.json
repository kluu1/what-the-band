{
  "name": "what-the-chuck-backend",
  "version": "1.0.0",
  "description": "Get Chuck Norris facts",
  "scripts": {
    "build": "tsc",
    "dev": "nodemon --exec serverless offline start --httpPort 3001",
    "test": "serverless-bundle test src/__tests__/*"
  },
  "dependencies": {
    "apollo-server-lambda": "^3.4.0",
    "axios": "^0.24.0"
  },
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "nodemon": "^2.0.14",
    "serverless-bundle": "^5.0.2",
    "serverless-offline": "^8.2.0"
  }
}

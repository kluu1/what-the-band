service: what-the-band
frameworkVersion: '2'

plugins:
  - serverless-offline
  - serverless-bundle

provider:
  name: aws
  runtime: nodejs14.x
  lambdaHashingVersion: '20201221'

functions:
  api:
    handler: src/app.handler
    events:
      - httpApi: '*'
